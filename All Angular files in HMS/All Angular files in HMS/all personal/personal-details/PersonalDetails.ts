export class PersonalDetails {
    // constructor (guestId: number, guestName:string, address: string, phoneNo: any, email: string, username: string, password: string){}
    guestId: number;
    guestName:string;
    address: string;
    phoneNo: any;
    email: string;
    username: string;
    password: string;
}